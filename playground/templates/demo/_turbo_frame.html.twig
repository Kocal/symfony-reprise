<turbo-frame id="detail">
    <div class="rounded-xl border border-slate-200 bg-slate-50 p-4">
        <h3 class="mt-0 text-lg font-semibold">{{ tool }}</h3>
        <p>{{ detail }}</p>
        <p class="mb-0 text-slate-500">Loaded into the frame at {{ now|date('H:i:s') }} — no full page reload.</p>
    </div>
</turbo-frame>
